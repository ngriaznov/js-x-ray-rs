//! Upstream: `src/ShadyLink.ts`
//!
//! PORT-TODO(stub): faithful port pending (URL/domain/IP suspicion checks).

pub struct ShadyLink;

impl ShadyLink {
    /// Whether the given URL/string is considered shady.
    pub fn is_shady_link(link: &str) -> bool {
        // PORT-TODO(stub)
        let _ = link;
        false
    }
}
