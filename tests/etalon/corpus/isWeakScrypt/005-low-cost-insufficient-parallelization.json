{
  "name": "(isWeakScrypt): should warn when cost option is set without sufficient parallelization (N=16384, default p=1)",
  "code": "\n        import crypto from 'crypto';\n        crypto.scrypt(password, salt, 64, { cost: 16384 }, (err, key) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.weak-scrypt"
    ]
  }
}
