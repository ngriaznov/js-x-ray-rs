{
  "name": "(log-usage probe > winston > winston logger): should not conflict with other traced return values [logger]",
  "code": "const winston = require(\"winston\");\n                    const logger = winston.createLogger();;\n                    (new vm.Script(code)).info(\"hello\");\n                    ",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
