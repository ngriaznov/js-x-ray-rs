{
  "name": "(log-usage probe > winston > winston logger > logger.child): should follow the consecutive assignment of the child logger function [createLogger]",
  "code": "const winston = require(\"winston\");\n                      const logger = winston.createLogger();\n                      const childLogger = logger.child({module: \"auth\"});\n                      const childLoggerBis = childLogger;\n                      const info = childLoggerBis.info;\n                      info(\"hello\");\n                      ",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
