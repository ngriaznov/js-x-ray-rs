{
  "name": "(log-usage probe > winston > winston logger > logger.child): should follow the consecutive assignment of the child logger [winston]",
  "code": "const winston = require(\"winston\");\n                      const logger = winston;\n                      const childLogger = logger.child({module: \"auth\"});\n                      const childLoggerBis = childLogger;\n                      childLoggerBis.info(\"hello\");\n                      ",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
