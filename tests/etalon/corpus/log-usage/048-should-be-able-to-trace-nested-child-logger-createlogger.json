{
  "name": "(log-usage probe > winston > winston logger > logger.child): should be able to trace nested child logger [createLogger]",
  "code": "const winston = require(\"winston\");\n                      const logger = winston.createLogger(undefined);\n                      const childLogger = logger.child({module: \"auth\"});\n                      const childLogger2 = childLogger.child({ module: \"something\"});\n                      const childLogger3 = childLogger2.child({ module: \"something2\"});\n                      const info = childLogger3.info;\n                      info(\"hello\");\n                      ",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
