{
  "name": "(isSyncIO probe): it should report a warning in case of *Sync(...params)` usage [statSync.js]",
  "code": "const { statSync } = require(\"fs\");\n\nstatSync('./test.txt');\n",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
