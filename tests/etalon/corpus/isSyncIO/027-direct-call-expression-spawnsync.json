{
  "name": "(isSyncIO probe): it should report a warning in case of *Sync(...params)` usage [spawnSync.js]",
  "code": "const { spawnSync } = require(\"child_process\");\n\nspawnSync(\"ls\",[\"-la\", \"/home\"], {encoding: \"utf-8\"});\n",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
