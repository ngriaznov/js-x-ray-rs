{
  "name": "(isSyncIO probe): it should report a warning in case of `[expression]*Sync(...params)` usage [fstatSync.js]",
  "code": "import fs from 'fs';\n\nconst fstatSync = fs.fstatSync;\n\nfstatSync(1);\n",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
