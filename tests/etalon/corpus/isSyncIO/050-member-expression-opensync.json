{
  "name": "(isSyncIO probe): it should report a warning in case of `[expression]*Sync(...params)` usage [openSync.js]",
  "code": "import fs from 'fs';\n\nconst openSync = fs.openSync;\n\nopenSync('test.txt', 'r');\n",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
