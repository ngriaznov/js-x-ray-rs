{
  "name": "(isSyncIO probe): it should report a warning in case of *Sync(...params)` usage [renameSync.js]",
  "code": "const { renameSync } = require(\"fs\");\n\nrenameSync('oldfile.txt', 'newfile.txt');\n",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
