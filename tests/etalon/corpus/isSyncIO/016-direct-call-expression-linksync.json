{
  "name": "(isSyncIO probe): it should report a warning in case of *Sync(...params)` usage [linkSync.js]",
  "code": "const { linkSync } = require(\"fs\");\n\nlinkSync('./source.txt', './destination.txt');\n",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
