{
  "name": "(isSyncIO probe): it should report a warning in case of `[expression]*Sync(...params)` usage [symlinkSync.js]",
  "code": "import fs from 'fs';\n\nconst symlinkSync = fs.symlinkSync;\n\nsymlinkSync('target.txt', 'symlink.txt');\n",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
