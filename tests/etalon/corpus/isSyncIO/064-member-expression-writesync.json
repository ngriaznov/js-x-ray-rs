{
  "name": "(isSyncIO probe): it should report a warning in case of `[expression]*Sync(...params)` usage [writeSync.js]",
  "code": "import fs from 'fs';\n\nconst writeSync = fs.writeSync;\n\nwriteSync('test.txt', 'Hello World!', 'utf8');\n",
  "analyserOptions": {
    "optionalWarnings": true
  }
}
