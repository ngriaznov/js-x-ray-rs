{
  "name": "(isUnsafePrehash probe): no warning (proper usage) - should not warn when crypto module is not imported",
  "code": "\n        import bcrypt from 'bcryptjs';\n        const crypto = { createHash() { return { update() { return this; }, digest() {} }; } };\n        bcrypt.hash(crypto.createHash('sha256').update(password).digest(), salt, (err, hash) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
