{
  "name": "(isUnsafePrehash probe): crypto.unsafe-prehash - should warn when bcryptjs.hash is imported as a named import",
  "code": "\n        import { hash } from 'bcryptjs';\n        import crypto from 'crypto';\n        hash(crypto.createHash('sha256').update(password).digest(), salt, (err, h) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
