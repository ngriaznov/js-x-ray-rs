{
  "name": "(isUnsafePrehash probe): no warning (proper usage) - should not warn when digest() uses base64 encoding",
  "code": "\n        import bcrypt from 'bcryptjs';\n        import crypto from 'crypto';\n        bcrypt.hash(crypto.createHash('sha256').update(password).digest('base64'), salt, (err, hash) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
