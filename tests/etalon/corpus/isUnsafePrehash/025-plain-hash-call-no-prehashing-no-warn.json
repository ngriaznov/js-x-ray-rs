{
  "name": "(isUnsafePrehash probe): no warning (proper usage) - should not warn for a plain bcryptjs.hash call without pre-hashing",
  "code": "\n        import bcrypt from 'bcryptjs';\n        import crypto from 'crypto';\n        bcrypt.hash(password, salt, (err, hash) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
