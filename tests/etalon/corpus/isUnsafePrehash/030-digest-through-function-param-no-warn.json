{
  "name": "(isUnsafePrehash probe): known limitations - should not warn when an unsafely pre-hashed digest reaches bcrypt through a function parameter",
  "code": "\n        import bcrypt from 'bcryptjs';\n        import crypto from 'crypto';\n\n        function hashPassword(prehashed) {\n          bcrypt.hash(prehashed, salt, callback);\n        }\n        hashPassword(crypto.createHash('sha256').update(password).digest());\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
