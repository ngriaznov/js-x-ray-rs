{
  "name": "(isUnsafePrehash probe): crypto.unsafe-prehash - should warn when digest('binary').toString('hex') (outer toString is a no-op) is passed through a variable",
  "code": "\n        import bcrypt from 'bcryptjs';\n        import crypto from 'crypto';\n        const prehashed = crypto.createHash('sha256').update(password).digest('binary').toString('hex');\n        bcrypt.hash(prehashed, salt, (err, hash) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
