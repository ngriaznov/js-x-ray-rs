{
  "name": "(isUnsafePrehash probe): no warning (proper usage) - should not warn when bcryptjs module is not imported",
  "code": "\n        import crypto from 'crypto';\n        const bcrypt = { hash() {} };\n        bcrypt.hash(crypto.createHash('sha256').update(password).digest(), salt, (err, hash) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
