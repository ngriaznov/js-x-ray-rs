{
  "name": "(isUnsafePrehash probe): crypto.unsafe-prehash - should warn when bcryptjs is imported as a namespace",
  "code": "\n        import * as bcrypt from 'bcryptjs';\n        import crypto from 'crypto';\n        bcrypt.hash(crypto.createHash('sha256').update(password).digest(), salt, (err, hash) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
