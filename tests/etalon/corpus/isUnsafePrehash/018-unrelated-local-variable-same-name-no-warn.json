{
  "name": "(isUnsafePrehash probe): variable indirection - should not warn when an unrelated local variable shares its name with an unsafe pre-hashed variable",
  "code": "\n        import bcrypt from 'bcryptjs';\n        import crypto from 'crypto';\n\n        function bar() {\n          const prehashed = someUnrelatedSafeValue();\n          bcrypt.hash(prehashed, salt, (err, hash) => {});\n        }\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
