{
  "name": "(isUnsafePrehash probe): variable indirection - should not warn when an unrelated function parameter shares its name with an unsafe pre-hashed variable",
  "code": "\n        import bcrypt from 'bcryptjs';\n        import crypto from 'crypto';\n\n        function bar(prehashed) {\n          bcrypt.hash(prehashed, salt, (err, hash) => {});\n        }\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
