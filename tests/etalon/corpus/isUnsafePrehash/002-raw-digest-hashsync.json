{
  "name": "(isUnsafePrehash probe): crypto.unsafe-prehash - should warn when a raw digest() (no encoding) is passed to bcryptjs.hashSync",
  "code": "\n        import bcrypt from 'bcryptjs';\n        import crypto from 'crypto';\n        const hash = bcrypt.hashSync(crypto.createHash('sha256').update(password).digest(), salt);\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
