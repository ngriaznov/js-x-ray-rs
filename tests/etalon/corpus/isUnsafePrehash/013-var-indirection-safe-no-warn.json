{
  "name": "(isUnsafePrehash probe): variable indirection - should not warn when a safely pre-hashed variable is passed to bcryptjs.hash",
  "code": "\n        import bcrypt from 'bcryptjs';\n        import crypto from 'crypto';\n        const prehashed = crypto.createHash('sha256').update(password).digest('base64');\n        bcrypt.hash(prehashed, salt, (err, hash) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.unsafe-prehash"
    ]
  }
}
