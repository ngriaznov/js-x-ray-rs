{
  "name": "it should return isOneLineRequire false given a single line CJS export with illegal callees (case 7): module.exports = require('constants') ? (require('constants') ? {} : {}) : (require('constants') ? {} : {});",
  "code": "module.exports = require('constants') ? (require('constants') ? {} : {}) : (require('constants') ? {} : {});"
}
