{
  "name": "it should return isOneLineRequire true given a single line CJS export with a valid assignment (case 2): module.exports = require('constants') ? require('fs') : require('foo');",
  "code": "module.exports = require('constants') ? require('fs') : require('foo');"
}
