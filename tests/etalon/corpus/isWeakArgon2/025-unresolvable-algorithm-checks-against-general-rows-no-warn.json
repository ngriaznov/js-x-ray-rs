{
  "name": "(isWeakArgon2): should check the parameters against the general OWASP rows when the algorithm is unresolvable",
  "code": "\n        import crypto from 'crypto';\n        crypto.argon2(algorithm, { memory: 47104, passes: 1 }, (err, tag) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.weak-argon2"
    ]
  }
}
