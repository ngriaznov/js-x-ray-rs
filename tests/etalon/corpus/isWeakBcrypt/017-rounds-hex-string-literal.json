{
  "name": "(isWeakBcrypt): should warn when rounds are stored in a constant assigned a hex string literal",
  "code": "\n        import bcrypt from 'bcryptjs';\n        const ROUNDS = \"0x5\";\n        bcrypt.hash(password, ROUNDS, (err, hash) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.weak-bcrypt"
    ]
  }
}
