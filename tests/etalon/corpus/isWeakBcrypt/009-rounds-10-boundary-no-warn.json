{
  "name": "(isWeakBcrypt): should not warn when rounds are exactly 10 (the OWASP minimum)",
  "code": "\n        import bcrypt from 'bcryptjs';\n        bcrypt.hash(password, 10, (err, hash) => {});\n      ",
  "analyserOptions": {
    "optionalWarnings": [
      "crypto.weak-bcrypt"
    ]
  }
}
